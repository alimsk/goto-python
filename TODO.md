- [x] encode function supports line decrease
- [ ] support 3.10
  - [ ] encode `co_linetable`
  - [ ] version check in `_get_instructions` and `patch` function

# Optional
- [ ] function to minimize line table